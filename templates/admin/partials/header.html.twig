    <nav class="navbar navbar-dark sticky-top bg-dark flex-md-nowrap p-0 shadow">
  <a class="navbar-brand col-md-3 col-lg-2 mr-0 px-3" href="#">Company name</a>
  <button class="navbar-toggler position-absolute d-md-none collapsed" type="button" data-toggle="collapse" data-target="#sidebarMenu" aria-controls="sidebarMenu" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <input class="form-control form-control-dark w-100" type="text" placeholder="Search" aria-label="Search">
  <ul class="navbar-nav px-3">
    <li class="nav-item text-nowrap">
      <a class="nav-link" href="{{path('app_logout')}}">Deconnecter</a>
    </li>
  </ul>
</nav>

{% for label, messages in app.flashes %}
    <div class="container" role="alert">
    {% for message in messages %}  
        <div class="alert alert-{{label}} mt-3 d-flex justify-content-center" role="alert">
            <p style="padding-top:16px"> {{message | raw}} </p>
        </div>
    {% endfor %}
    </div>   
{% endfor %}

<div class="container-fluid">
  <div class="row">
    <nav id="sidebarMenu" class="col-md-3 col-lg-2 d-md-block bg-light sidebar collapse">
      <div class="sidebar-sticky pt-3">
      <h6 class="sidebar-heading d-flex justify-content-between align-items-center px-3 mt-1 mb-1 text-muted">
          <span>GESTION</span>
        </h6>
        <ul class="nav flex-column">
          <li class="nav-item">
            <a class="nav-link {% if current_menu == 'dashboard' %} active {% endif %}" href="{{path('admin_dashboard')}}">
              <span data-feather="home"></span>
              Dashboard <span class="sr-only">(current)</span>
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="{{path('admin_calendar')}}">
              <span data-feather="calendar"></span>
              Calendrier
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link {% if current_menu == 'booking' %} active {% endif %}" href="{{path('admin_booking')}}">
              <span data-feather="book-open"></span>
              Reservation {% if bookingService.getNotConfirmBooking|length != 0  %} <span class="badge badge-pill badge-danger">{{bookingService.getNotConfirmBooking|length}}</span> {% endif %}
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link {% if current_menu == 'customer' %} active {% endif %}" href="{{path('admin_customer')}}">
              <span data-feather="users"></span>
              Clients
            </a>
          </li>
        </ul>

        <h6 class="sidebar-heading d-flex justify-content-between align-items-center px-3 mt-4 mb-1 text-muted">
          <span>CONFIGURATION</span>
        </h6>
        <ul class="nav flex-column mb-2">
          <li class="nav-item">
            <a class="nav-link" href="#">
              <span data-feather="dollar-sign"></span>
              Prix
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="#">
              <span data-feather="sun"></span>
              Saison
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="#">
              <span data-feather="at-sign"></span>
              Site
            </a>
          </li>
        </ul>
      </div>
    </nav>
    
